<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8"/>
<meta content="width=device-width, initial-scale=1.0" name="viewport"/>
<title>Water Service Bill | <%= invoice.id %></title>
<script src="https://cdn.tailwindcss.com?plugins=forms,typography"></script>
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&amp;display=swap" rel="stylesheet"/>
<script>
    tailwind.config = {
      darkMode: "class",
      theme: {
        extend: {
          colors: {
            // RED/MAROON PRIMARY COLOR
            primary: "#8B0000",
            "background-light": "#FFFFFF",
            "background-dark": "#121212",
            "surface-light": "#F5F5F5", 
            "surface-dark": "#1E1E1E",
            "text-light": "#000000",
            "text-dark": "#FFFFFF",
            "text-secondary-light": "#6B7280",
            "text-secondary-dark": "#9CA3AF",
          },
          fontFamily: {
            display: ["Roboto", "sans-serif"],
          },
          borderRadius: {
            DEFAULT: "0.5rem",
          },
        },
      },
    };
  </script>
<style>
    body {
      font-family: 'Roboto', sans-serif;
    }
    /* CRITICAL PRINT STYLES FOR SINGLE-PAGE PDF */
    @media print {
        section { 
            page-break-inside: avoid;
        }
        /* Maximizes container width */
        .container-full { width: 100% !important; margin: 0 !important; padding: 0 !important; }
        
        /* Font and Spacing Adjustments */
        .text-xs { font-size: 0.7rem !important; }
        .text-sm { font-size: 0.8rem !important; }
        .text-base { font-size: 0.9rem !important; }
        .p-4 { padding: 0.5rem !important; }
        .p-6 { padding: 0.75rem !important; }

        /* Force colors */
        .bg-primary { background-color: #8B0000 !important; -webkit-print-color-adjust: exact; color-adjust: exact; }
        .bg-primary\/10 { background-color: #f5eaea !important; -webkit-print-color-adjust: exact; color-adjust: exact; }
        .text-primary { color: #8B0000 !important; }
        .text-white { color: #FFFFFF !important; }
        .shadow-lg { box-shadow: none !important; }
    }
  </style>
</head>
<body class="bg-background-light dark:bg-background-dark text-text-light dark:text-text-dark font-display">
<div class="container-full mx-auto p-4 sm:p-8"> <div class="bg-card-light dark:bg-card-dark rounded-lg shadow-lg overflow-hidden">

<header class="bg-primary text-white p-6 flex justify-between items-center">
<div>
<h1 class="text-2xl font-bold">Water Service Bill</h1>
<p class="text-sm">Simplified Tax Invoice (VAT Number: 300184159100003)</p>
</div>
<!-- <div class="text-right">
    <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="white" viewBox="0 0 24 24">
        <path d="M12 0c-4.198 0-8 3.403-8 7.602 0 4.198 3.847 9.873 8 16.398 4.153-6.526 8-12.2 8-16.398 0-4.199-3.802-7.602-8-7.602zm0 11c-1.657 0-3-1.343-3-3s1.343-3 3-3 3 1.343 3 3-1.343 3-3 3z"/>
    </svg>
</div> -->
</header>
<main class="p-6"> 

<section class="grid grid-cols-2 gap-8 mb-6">
<div>
<h2 class="text-lg font-bold text-primary mb-3 border-b border-gray-300 dark:border-gray-700 pb-1">Customer Information</h2>
<div class="grid grid-cols-2 gap-x-2 gap-y-1 text-sm"> 
<span class="font-semibold text-text-secondary-light dark:text-text-secondary-dark">Name:</span>
<span class="text-right"><%= invoice.customerName %></span>
<span class="font-semibold text-text-secondary-light dark:text-text-secondary-dark">Address:</span>
<span class="text-right"><%= invoice.address %></span>
<span class="font-semibold text-text-secondary-light dark:text-text-secondary-dark">Account Number:</span>
<span class="text-right"><%= invoice.accountNumber %></span>
<span class="font-semibold text-text-secondary-light dark:text-text-secondary-dark">Meter Number:</span>
<span class="text-right"><%= invoice.meterNumber %></span>
<span class="font-semibold text-text-secondary-light dark:text-text-secondary-dark">Meter Diameter:</span>
<span class="text-right"><%= invoice.meterDiameter %></span>
</div>
</div>
<div>
<h2 class="text-lg font-bold text-primary mb-3 border-b border-gray-300 dark:border-gray-700 pb-1">Bill Details</h2>
<div class="grid grid-cols-2 gap-x-2 gap-y-1 text-sm"> 
<span class="font-semibold text-text-secondary-light dark:text-text-secondary-dark">Bill Number:</span>
<span class="text-right"><%= invoice.accountNumber %></span>
<span class="font-semibold text-text-secondary-light dark:text-text-secondary-dark">Bill Issue Date:</span>
<span class="text-right"><%= invoice.issueDate %></span>
<span class="font-semibold text-text-secondary-light dark:text-text-secondary-dark">Due Date:</span>
<span class="text-right"><%= invoice.dueDate %></span>
<span class="font-semibold text-text-secondary-light dark:text-text-secondary-dark">Bill Period:</span>
<span class="text-right">30 Days</span>
</div>
</div>
</section>

<section class="mb-6 p-4 rounded-lg border border-gray-200 dark:border-gray-700">
<h2 class="text-lg font-bold text-primary mb-3">Consumption Details</h2>
<div class="grid grid-cols-3 gap-4 text-center">
<div class="bg-primary/10 dark:bg-primary/20 p-3 rounded-lg">
<p class="text-text-secondary-light dark:text-text-secondary-dark text-sm">Start Consumption Date</p>
<p class="font-semibold text-base"><%= invoice.consumptionStartDate %></p>
</div>
<div class="bg-primary/10 dark:bg-primary/20 p-3 rounded-lg">
<p class="text-text-secondary-light dark:text-text-secondary-dark text-sm">End Consumption Date</p>
<p class="font-semibold text-base"><%= invoice.consumptionEndDate %></p>
</div>
<div class="bg-primary/10 dark:bg-primary/20 p-3 rounded-lg">
<p class="text-text-secondary-light dark:text-text-secondary-dark text-sm">Total Consumption</p>
<p class="font-semibold text-base"><%= Number(consumption).toFixed(0) %> gal</p>
</div>
</div>
</section>

<section class="mb-6">
<h2 class="text-lg font-bold text-primary mb-3">Billing Summary</h2>
<div class="overflow-x-auto">
<table class="w-full text-sm text-left">
<thead class="bg-gray-100 dark:bg-gray-800">
<tr>
<th class="p-2 font-semibold">Bill Information</th>
<th class="p-2 font-semibold text-right">Before VAT (SAR)</th>
<th class="p-2 font-semibold text-right">VAT (15%)</th>
<th class="p-2 font-semibold text-right">After VAT (SAR)</th>
</tr>
</thead>
<tbody>
<% 
    const waterBefore = Number(invoice.consumptionValue || 0);
    const sewerBefore = Number(invoice.serviceFee || 0);
    const tariffBefore = Number(invoice.meterTariff || 0);

    const waterVAT = (waterBefore * 0.15).toFixed(2);
    const sewerVAT = (sewerBefore * 0.15).toFixed(2);
    const tariffVAT = (tariffBefore * 0.15).toFixed(2);
    
    const waterAfter = (waterBefore * 1.15).toFixed(2);
    const sewerAfter = (sewerBefore * 1.15).toFixed(2);
    const tariffAfter = (tariffBefore * 1.15).toFixed(2);

    const totalBefore = (waterBefore + sewerBefore + tariffBefore).toFixed(2);
    const totalVAT = (Number(waterVAT) + Number(sewerVAT) + Number(tariffVAT)).toFixed(2);
    const totalAfter = Number(invoice.totalAmount).toFixed(2); 
%>
<tr class="border-b border-gray-200 dark:border-gray-700">
<td class="p-2">Water Consumption Value</td>
<td class="p-2 text-right"><%= waterBefore.toFixed(2) %></td>
<td class="p-2 text-right"><%= waterVAT %></td>
<td class="p-2 text-right"><%= waterAfter %></td>
</tr>
<tr class="border-b border-gray-200 dark:border-gray-700">
<td class="p-2">Sewer Service</td>
<td class="p-2 text-right"><%= sewerBefore.toFixed(2) %></td>
<td class="p-2 text-right"><%= sewerVAT %></td>
<td class="p-2 text-right"><%= sewerAfter %></td>
</tr>
<tr class="border-b border-gray-200 dark:border-gray-700">
<td class="p-2">Meter Tariff Fees</td>
<td class="p-2 text-right"><%= tariffBefore.toFixed(2) %></td>
<td class="p-2 text-right"><%= tariffVAT %></td>
<td class="p-2 text-right"><%= tariffAfter %></td>
</tr>
</tbody>
<tfoot>
<tr class="font-bold">
<td class="p-2">Total (SAR)</td>
<td class="p-2 text-right"><%= totalBefore %></td>
<td class="p-2 text-right"><%= totalVAT %></td>
<td class="p-2 text-right"><%= totalAfter %></td>
</tr>
</tfoot>
</table>
</div>
<p class="text-xs text-text-secondary-light dark:text-text-secondary-dark mt-2">The value of using sewerage services is equal to 50% of the value of water consumption. The unit of measurement is the cubic meter. The amount is subject to value added tax.</p>
</section>

<section class="grid grid-cols-2 gap-6 items-center mb-6">
<div class="bg-primary/10 dark:bg-primary/20 p-4 rounded-lg text-center">
<p class="text-text-secondary-light dark:text-text-secondary-dark text-sm">Current Consumption is equivalent to</p>
<p class="text-4xl font-bold text-primary my-1"><%= (consumption / 4755.1).toFixed(2) %></p>
<p class="text-base">Tankers (~4,755 gal per tanker)</p>
<div class="flex justify-center items-center mt-2">
<span class="text-5xl text-primary opacity-80">🚚</span> 
</div>
</div>
<div class="bg-primary text-white p-4 rounded-lg text-center shadow-lg">
<p class="text-base opacity-80">Total Amount (SAR)</p>
<p class="text-5xl font-bold my-1"><%= totalAfter %></p>
<div class="text-xs mt-2 whitespace-nowrap">
<span>Due Start Date: <%= invoice.issueDate %></span>
<span class="mx-2">|</span>
<span>Payment Deadline: <%= invoice.dueDate %></span> 
</div>
</div>
</section>
</main>

<footer class="p-3 text-center text-xs text-text-secondary-light dark:text-text-secondary-dark border-t border-gray-200 dark:border-gray-700">
<p class="text-[0.7rem] text-text-secondary-light dark:text-text-secondary-dark">
    This is a system generated invoice. For questions, contact <a class="text-primary hover:underline" href="mailto:reva.support@revartix.com">reva.support@revartix.com</a>
</p>
<!-- <p class="text-xs mt-1">reva.support@revartix.com</p> -->
</footer>

</div>
</div>
</body>
</html>