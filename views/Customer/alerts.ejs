<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alerts & Notifications | Reva Platform</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script> tailwind.config = { darkMode: 'class' } </script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <style> body { font-family: 'Inter', sans-serif; } </style>
</head>
<body class="bg-slate-100 dark:bg-slate-900 text-slate-800 dark:text-slate-200 transition-colors duration-300">
    <div class="flex">
        <%- include('partials/sidebar') %>
        <main class="flex-1 h-screen overflow-y-auto p-6 md:p-8">
            <header class="flex justify-between items-center mb-8">
                <div>
                    <h1 class="text-3xl font-bold text-slate-900 dark:text-white">Alerts & Notifications</h1>
                    <p class="text-slate-500 dark:text-slate-400 mt-1">Important updates regarding your account and services.</p>
                </div>
                <div class="flex items-center gap-4">
                    <div class="relative" id="user-dropdown">
                        <button id="dropdown-toggle" class="flex items-center gap-2 p-2 rounded-lg hover:bg-slate-200 dark:hover:bg-slate-700">
                            <span class="font-semibold text-sm"><%= user.name %></span>
                            <i class="fas fa-chevron-down text-xs"></i>
                        </button>
                        <div id="dropdown-menu" class="absolute hidden right-0 mt-2 w-48 bg-white dark:bg-slate-800 rounded-lg shadow-xl z-10 border border-slate-200 dark:border-slate-700 py-1">
                            <a href="/logout" class="flex items-center gap-3 px-4 py-2 text-sm text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-700">
                                <i class="fa-solid fa-arrow-right-from-bracket"></i>
                                <span>Logout</span>
                            </a>
                        </div>
                    </div>
                    <button id="theme-toggle" class="w-10 h-10 flex items-center justify-center rounded-lg hover:bg-slate-200 dark:hover:bg-slate-700">
                        <i class="fas fa-moon"></i>
                    </button>
                </div>
            </header>

            <section class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                <div class="bg-white dark:bg-slate-800 rounded-xl shadow-md p-6 flex items-center gap-5">
                    <div class="w-12 h-12 flex-shrink-0 flex items-center justify-center rounded-full bg-slate-100 dark:bg-slate-700">
                        <i class="fa-solid fa-bell text-xl text-slate-500 dark:text-slate-300"></i>
                    </div>
                    <div>
                        <p class="text-3xl font-bold text-slate-800 dark:text-slate-100"><%= kpis.total %></p>
                        <p class="text-sm font-medium text-slate-500 dark:text-slate-400">Total Notifications</p>
                    </div>
                </div>
                <div class="bg-white dark:bg-slate-800 rounded-xl shadow-md p-6 flex items-center gap-5">
                    <div class="w-12 h-12 flex-shrink-0 flex items-center justify-center rounded-full bg-red-100 dark:bg-red-900/50">
                        <i class="fa-solid fa-triangle-exclamation text-xl text-red-500"></i>
                    </div>
                    <div>
                        <p class="text-3xl font-bold text-slate-800 dark:text-slate-100"><%= kpis.urgent %></p>
                        <p class="text-sm font-medium text-slate-500 dark:text-slate-400">Urgent Alerts</p>
                    </div>
                </div>
                <div class="bg-white dark:bg-slate-800 rounded-xl shadow-md p-6 flex items-center gap-5">
                     <div class="w-12 h-12 flex-shrink-0 flex items-center justify-center rounded-full bg-sky-100 dark:bg-sky-900/50">
                        <i class="fa-solid fa-info-circle text-xl text-sky-500"></i>
                    </div>
                    <div>
                        <p class="text-3xl font-bold text-slate-800 dark:text-slate-100"><%= kpis.info %></p>
                        <p class="text-sm font-medium text-slate-500 dark:text-slate-400">Informational</p>
                    </div>
                </div>
            </section>

            <section class="space-y-6">
                <% if (alerts && alerts.length > 0) { %>
                    <% alerts.forEach(alert => { %>
                        <% 
                            let colors = { border: 'border-slate-500', iconBg: 'bg-slate-100 dark:bg-slate-700', iconText: 'text-slate-500 dark:text-slate-300', icon: 'fa-info-circle' };
                            if (alert.type === 'overdue') {
                                colors = { border: 'border-red-500', iconBg: 'bg-red-100 dark:bg-red-900/50', iconText: 'text-red-500', icon: 'fa-file-invoice-dollar' };
                            } else if (alert.type === 'usage') {
                                colors = { border: 'border-yellow-500', iconBg: 'bg-yellow-100 dark:bg-yellow-900/50', iconText: 'text-yellow-500', icon: 'fa-triangle-exclamation' };
                            } else if (alert.type === 'service') {
                                colors = { border: 'border-sky-500', iconBg: 'bg-sky-100 dark:bg-sky-900/50', iconText: 'text-sky-500', icon: 'fa-wrench' };
                            }
                        %>
                        <div class="bg-white dark:bg-slate-800 rounded-lg shadow-md p-5 flex items-start gap-4 border-l-4 <%= colors.border %>">
                            <div class="w-10 h-10 flex-shrink-0 flex items-center justify-center rounded-full <%= colors.iconBg %>">
                                <i class="fa-solid <%= colors.icon %> <%= colors.iconText %>"></i>
                            </div>
                            <div>
                                <div class="flex items-center justify-between">
                                    <h2 class="text-lg font-semibold text-slate-900 dark:text-white"><%= alert.title %></h2>
                                    <p class="text-xs text-slate-500 dark:text-slate-400"><%= alert.timestamp %></p>
                                </div>
                                <p class="text-sm text-slate-600 dark:text-slate-300 mt-1"><%= alert.message %></p>
                                <% if (alert.link) { %>
                                    <div class="mt-3">
                                        <a href="<%= alert.link %>" class="text-sm font-semibold text-[#C0392B] hover:underline">
                                            <%= alert.linkText %> <i class="fa-solid fa-arrow-right text-xs ml-1"></i>
                                        </a>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="text-center py-16 px-6 bg-white dark:bg-slate-800 rounded-lg shadow-md">
                        <i class="fa-solid fa-bell-slash text-5xl text-slate-400 dark:text-slate-500"></i>
                        <h2 class="mt-4 text-xl font-semibold text-slate-800 dark:text-slate-200">No New Notifications</h2>
                        <p class="mt-2 text-slate-500 dark:text-slate-400">You're all caught up! We'll notify you here when there's something new.</p>
                    </div>
                <% } %>
            </section>
        </main>
    </div>
    <%- include('partials/scripts') %>
</body>
</html>