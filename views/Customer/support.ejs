<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Support & Help | Reva Platform</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script> tailwind.config = { darkMode: 'class' } </script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <style> 
        body { font-family: 'Inter', sans-serif; }
        .faq-answer {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease-out;
        }
    </style>
</head>
<body class="bg-slate-100 dark:bg-slate-900 text-slate-800 dark:text-slate-200 transition-colors duration-300">
    <div class="flex">
        <%- include('partials/sidebar') %>
        <main class="flex-1 h-screen overflow-y-auto p-6 md:p-8">
            <header class="flex justify-between items-center mb-8">
                <div>
                    <h1 class="text-3xl font-bold text-slate-900 dark:text-white">Support & Help Center</h1>
                    <p class="text-slate-500 dark:text-slate-400 mt-1">Find answers, get help, and manage your services.</p>
                </div>
                <div class="flex items-center gap-4">
                    <div class="relative" id="user-dropdown">
                        <button id="dropdown-toggle" class="flex items-center gap-2 p-2 rounded-lg hover:bg-slate-200 dark:hover:bg-slate-700">
                            <span class="font-semibold text-sm"><%= user.name %></span>
                            <i class="fas fa-chevron-down text-xs"></i>
                        </button>
                        <div id="dropdown-menu" class="absolute hidden right-0 mt-2 w-48 bg-white dark:bg-slate-800 rounded-lg shadow-xl z-10 border border-slate-200 dark:border-slate-700 py-1">
                            <a href="/logout" class="flex items-center gap-3 px-4 py-2 text-sm text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-700">
                                <i class="fa-solid fa-arrow-right-from-bracket"></i>
                                <span>Logout</span>
                            </a>
                        </div>
                    </div>
                    <button id="theme-toggle" class="w-10 h-10 flex items-center justify-center rounded-lg hover:bg-slate-200 dark:hover:bg-slate-700">
                        <i class="fas fa-moon"></i>
                    </button>
                </div>
            </header>

            <section class="space-y-8">
                <div class="bg-white dark:bg-slate-800 rounded-xl shadow-md p-6">
                    <h2 class="text-xl font-semibold text-slate-900 dark:text-white mb-4">Contact Support</h2>
                    <p class="text-sm text-slate-500 dark:text-slate-400 mb-6">Our team is here to help. Choose your preferred way to connect with us.</p>
                    <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 text-center">
                        <a href="#" id="live-chat-btn" class="block p-6 bg-slate-50 dark:bg-slate-700/50 rounded-lg hover:bg-slate-100 dark:hover:bg-slate-700">
                            <i class="fa-solid fa-comments text-3xl text-[#C0392B]"></i>
                            <p class="mt-3 font-semibold text-slate-800 dark:text-slate-200">Live Chat</p>
                        </a>
                        <a href="#" id="submit-ticket-btn" class="block p-6 bg-slate-50 dark:bg-slate-700/50 rounded-lg hover:bg-slate-100 dark:hover:bg-slate-700">
                            <i class="fa-solid fa-ticket text-3xl text-[#C0392B]"></i>
                            <p class="mt-3 font-semibold text-slate-800 dark:text-slate-200">Submit a Ticket</p>
                        </a>
                        <a href="https://wa.me/966555123456?text=I%20need%20support%20regarding%20my%20Reva%20Platform%20account." target="_blank" class="block p-6 bg-slate-50 dark:bg-slate-700/50 rounded-lg hover:bg-slate-100 dark:hover:bg-slate-700">
                            <i class="fa-brands fa-whatsapp text-3xl text-[#C0392B]"></i>
                            <p class="mt-3 font-semibold text-slate-800 dark:text-slate-200">WhatsApp</p>
                        </a>
                    </div>
                </div>

                <div class="bg-white dark:bg-slate-800 rounded-xl shadow-md p-6">
                    <h2 class="text-xl font-semibold text-slate-900 dark:text-white mb-4">Service Requests</h2>
                    <ul class="divide-y divide-slate-200 dark:divide-slate-700 service-request-list"> <li class="py-4 flex items-center justify-between">
                            <div class="flex items-center gap-4">
                                <i class="fa-solid fa-plug-circle-plus text-2xl text-slate-400"></i>
                                <div>
                                    <h3 class="font-semibold text-slate-800 dark:text-slate-200">Request a New Connection</h3>
                                    <p class="text-sm text-slate-500 dark:text-slate-400">Apply for a new water service connection for your property.</p>
                                </div>
                            </div>
                            <button class="px-4 py-2 text-sm font-semibold bg-slate-100 dark:bg-slate-700 rounded-lg hover:bg-slate-200 dark:hover:bg-slate-600 start-request-btn">Start Request</button>
                        </li>
                        <li class="py-4 flex items-center justify-between">
                            <div class="flex items-center gap-4">
                                <i class="fa-solid fa-plug-circle-minus text-2xl text-slate-400"></i>
                                <div>
                                    <h3 class="font-semibold text-slate-800 dark:text-slate-200">Request Disconnection</h3>
                                    <p class="text-sm text-slate-500 dark:text-slate-400">Submit a request to permanently disconnect service.</p>
                                </div>
                            </div>
                            <button class="px-4 py-2 text-sm font-semibold bg-slate-100 dark:bg-slate-700 rounded-lg hover:bg-slate-200 dark:hover:bg-slate-600 start-request-btn">Start Request</button>
                        </li>
                         <li class="py-4 flex items-center justify-between">
                            <div class="flex items-center gap-4">
                                <i class="fa-solid fa-gauge-circle-bolt text-2xl text-slate-400"></i>
                                <div>
                                    <h3 class="font-semibold text-slate-800 dark:text-slate-200">Request Meter Replacement</h3>
                                    <p class="text-sm text-slate-500 dark:text-slate-400">Report a faulty meter and request a replacement unit.</p>
                                </div>
                            </div>
                            <button class="px-4 py-2 text-sm font-semibold bg-slate-100 dark:bg-slate-700 rounded-lg hover:bg-slate-200 dark:hover:bg-slate-600 start-request-btn">Start Request</button>
                        </li>
                    </ul>
                </div>

                <div class="bg-white dark:bg-slate-800 rounded-xl shadow-md p-6">
                    <h2 class="text-xl font-semibold text-slate-900 dark:text-white mb-4">Frequently Asked Questions</h2>
                    <div class="space-y-4">
                        <% faqs.forEach(faq => { %>
                            <div class="border-b border-slate-200 dark:border-slate-700 pb-4">
                                <button class="faq-toggle w-full flex justify-between items-center text-left">
                                    <span class="font-semibold text-slate-800 dark:text-slate-200"><%= faq.question %></span>
                                    <i class="fa-solid fa-chevron-down transition-transform"></i>
                                </button>
                                <div class="faq-answer mt-2 text-slate-600 dark:text-slate-300">
                                    <p><%= faq.answer %></p>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                </div>
            </section>
        </main>
    </div>

<div id="request-modal" class="fixed inset-0 z-50 hidden items-center justify-center bg-black bg-opacity-50 transition-opacity duration-300 opacity-0">
    <div class="bg-white dark:bg-slate-800 rounded-xl shadow-2xl w-full max-w-lg m-4 transform transition-all duration-300 scale-95">
        <div class="p-6">
            <div class="flex justify-between items-center border-b border-slate-200 dark:border-slate-700 pb-3 mb-4">
                <h3 class="text-xl font-bold text-slate-900 dark:text-white" id="modal-title">Start Service Request</h3>
                <button type="button" class="text-slate-400 hover:text-slate-600 dark:hover:text-slate-300 modal-close">
                    <i class="fa-solid fa-xmark text-2xl"></i>
                </button>
            </div>
            
            <form id="service-request-form">
                <p class="text-sm text-slate-600 dark:text-slate-400 mb-4" id="modal-description">Please fill in the details below for your request.</p>

                <input type="hidden" id="request-type-input" name="requestType">
                
                <div class="space-y-4">
                    <div id="ticket-subject-field" style="display:none;">
                        <label for="ticket-subject" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1">Subject <span class="text-red-500">*</span></label>
                        <input type="text" id="ticket-subject" name="ticketSubject" 
                               class="w-full px-3 py-2 border border-slate-300 dark:border-slate-600 rounded-lg bg-slate-50 dark:bg-slate-700 text-slate-900 dark:text-slate-200 focus:ring-red-600 focus:border-red-600" 
                               placeholder="e.g., Billing Inquiry, Technical Issue">
                    </div>

                    <div>
                        <label for="contact-number" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1">Contact Number <span class="text-red-500">*</span></label>
                        <input type="tel" id="contact-number" name="contactNumber" required 
                               class="w-full px-3 py-2 border border-slate-300 dark:border-slate-600 rounded-lg bg-slate-50 dark:bg-slate-700 text-slate-900 dark:text-slate-200 focus:ring-red-600 focus:border-red-600" 
                               placeholder="e.g., +9665XXXXXXXX">
                    </div>
                    
                    <div id="preferred-date-field">
                        <label for="preferred-date" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1">Preferred Service Date (Optional)</label>
                        <input type="date" id="preferred-date" name="preferredDate"
                               class="w-full px-3 py-2 border border-slate-300 dark:border-slate-600 rounded-lg bg-slate-50 dark:bg-slate-700 text-slate-900 dark:text-slate-200 focus:ring-red-600 focus:border-red-600">
                    </div>

                    <div>
                        <label for="description" class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1">Detailed Description <span class="text-red-500">*</span></label>
                        <textarea id="description" name="description" rows="4" required 
                                  class="w-full px-3 py-2 border border-slate-300 dark:border-slate-600 rounded-lg bg-slate-50 dark:bg-slate-700 text-slate-900 dark:text-slate-200 focus:ring-red-600 focus:border-red-600" 
                                  placeholder="Provide details about your request or issue..."></textarea>
                    </div>
                </div>
                
                <div class="mt-6 flex justify-end gap-3">
                    <button type="button" class="px-4 py-2 text-sm font-semibold rounded-lg border border-slate-300 dark:border-slate-600 text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-700 modal-close">
                        Cancel
                    </button>
                    <button type="submit" class="px-4 py-2 text-sm font-semibold text-white bg-red-600 rounded-lg hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2 dark:focus:ring-offset-slate-800">
                        Submit
                    </button>
                </div>
            </form>

            <div id="success-message" class="hidden text-center p-8">
                <i class="fa-solid fa-circle-check text-5xl text-green-500 mb-4"></i>
                <h4 class="text-xl font-bold text-slate-900 dark:text-white mb-2">Submitted!</h4>
                <p class="text-slate-600 dark:text-slate-400">Your request type: <span id="submitted-request-type" class="font-semibold"></span> has been successfully submitted. We will contact you soon with an update. **Reference ID: <span id="request-ref-id" class="font-bold">SR-<%= Math.floor(Math.random() * 9000) + 1000 %></span>**</p>
            </div>
        </div>
    </div>
</div>
<%- include('partials/scripts') %>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const faqToggles = document.querySelectorAll('.faq-toggle');
            const startRequestButtons = document.querySelectorAll('.start-request-btn');
            const submitTicketButton = document.getElementById('submit-ticket-btn');
            const liveChatButton = document.getElementById('live-chat-btn'); // New target
            
            const modal = document.getElementById('request-modal');
            const modalTitle = document.getElementById('modal-title');
            const modalDescription = document.getElementById('modal-description');
            const requestTypeInput = document.getElementById('request-type-input');
            const form = document.getElementById('service-request-form');
            const successMessage = document.getElementById('success-message');
            const submittedRequestType = document.getElementById('submitted-request-type');

            // Conditional fields
            const ticketSubjectField = document.getElementById('ticket-subject-field');
            const ticketSubjectInput = document.getElementById('ticket-subject');
            const preferredDateField = document.getElementById('preferred-date-field');
            const preferredDateInput = document.getElementById('preferred-date');


            // --- FAQ Toggle Logic (Existing) ---
            faqToggles.forEach(toggle => {
                toggle.addEventListener('click', () => {
                    const answer = toggle.nextElementSibling;
                    const icon = toggle.querySelector('i');

                    if (answer.style.maxHeight) {
                        answer.style.maxHeight = null;
                        icon.classList.remove('rotate-180');
                    } else {
                        answer.style.maxHeight = answer.scrollHeight + "px";
                        icon.classList.add('rotate-180');
                    }
                });
            });

            // --- Modal Logic ---
            
            const showModal = (requestType, isGeneralTicket = false) => {
                // Reset and prepare the form state
                form.style.display = 'block';
                successMessage.style.display = 'none';
                form.reset();

                modalTitle.textContent = isGeneralTicket ? 'Submit a Support Ticket' : requestType;
                modalDescription.textContent = isGeneralTicket ? 
                    'Please describe your issue or inquiry below. We will assign a support agent shortly.' :
                    `You are submitting a request for: ${requestType}. Please confirm your contact details and provide a description.`;
                
                requestTypeInput.value = requestType;

                // Adjust fields based on request type
                if (isGeneralTicket) {
                    ticketSubjectField.style.display = 'block';
                    ticketSubjectInput.setAttribute('required', 'required');
                    preferredDateField.style.display = 'none';
                    preferredDateInput.removeAttribute('required');
                } else {
                    ticketSubjectField.style.display = 'none';
                    ticketSubjectInput.removeAttribute('required');
                    preferredDateField.style.display = 'block';
                    // Preferred date is optional for service requests
                    preferredDateInput.removeAttribute('required'); 
                }

                modal.classList.remove('hidden');
                setTimeout(() => {
                    modal.classList.add('opacity-100');
                    modal.querySelector('.max-w-lg').classList.remove('scale-95');
                }, 10);
            };

            const hideModal = () => {
                modal.classList.remove('opacity-100');
                modal.querySelector('.max-w-lg').classList.add('scale-95');
                setTimeout(() => {
                    modal.classList.add('hidden');
                }, 300);
            };

            // 1. Service Request Buttons (Existing logic)
            startRequestButtons.forEach(button => {
                button.addEventListener('click', (e) => {
                    e.preventDefault();
                    const listItem = button.closest('li');
                    const requestTitle = listItem.querySelector('h3').textContent.trim();
                    showModal(requestTitle, false);
                });
            });

            // 2. Submit a Ticket Button (New logic)
            submitTicketButton.addEventListener('click', (e) => {
                e.preventDefault();
                showModal('Support Ticket', true);
            });
            
            // 3. Live Chat Button (New logic)
            liveChatButton.addEventListener('click', (e) => {
                e.preventDefault();
                alert("Simulating Live Chat: Connecting you with an agent now...");
                // In a real app, this would open a new tab/iframe with the chat widget
            });


            // Close modal listeners
            document.querySelectorAll('.modal-close').forEach(button => {
                button.addEventListener('click', hideModal);
            });
            
            modal.addEventListener('click', (e) => {
                if (e.target === modal) {
                    hideModal();
                }
            });

            // Form submission logic (Updated to handle both service and ticket submissions)
            form.addEventListener('submit', (e) => {
                e.preventDefault();
                const formData = new FormData(form);
                const requestType = formData.get('requestType');
                const subject = formData.get('ticketSubject');

                console.log('--- Submitting Request ---');
                for (const pair of formData.entries()) {
                    console.log(`${pair[0]}: ${pair[1]}`);
                }
                
                // Determine the success message content
                const typeText = subject ? subject : requestType;

                // Simulate a successful submission
                form.style.display = 'none';
                submittedRequestType.textContent = typeText;
                successMessage.style.display = 'block';
            });
        });
    </script>
    <footer class="fixed bottom-4 left-4 text-xs text-slate-500 dark:text-slate-400">
        <p>© REVARTIX Confidential, REVARTIX, All Rights Reserved. 2025.</p>
        <p>www.revartix.com | reva.zone@revartix.com</p>
    </footer>
</body>
</html>