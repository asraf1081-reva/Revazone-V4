# Database Configuration
DB_USER=reva_user
DB_PASSWORD=reva_password
DB_NAME=reva_db
DB_HOST=db
DB_PORT=5432

# PostgreSQL Connection (used by pg package)
# Format: postgresql://user:password@host:port/database
PGUSER=reva_user
PGPASSWORD=reva_password
PGDATABASE=reva_db
PGHOST=db
PGPORT=5432

# Application Configuration
PORT=4000
NODE_ENV=production

# Session Secret (change this to a secure random string)
SESSION_SECRET=a-secure-secret-key-for-revartix-change-this-in-production

# Email/SMTP Configuration (Optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false

# Contact Email
CONTACT_EMAIL=reva.zone@revartix.com

# API Configuration (Optional)
METER_API_BASE=
METER_API_HEALTH_URL=
JOBS_HEALTH_URL=

# TLS Check (Optional)
TLS_CHECK_HOST=

